# Build021.2 KataGo JSON 與控制列修正版

- 修正 `/analyze` 的 moves 與 initial_stones JSON 格式。
- 新增 next_player、board_size、max_visits 正確欄位。
- 修正 HTTP 422 與 Demo fallback。
- 修正指定手數、前往、棋號顯示與播放列重疊。
- 直式與橫式 iPad 重新配置控制區高度。
- 保留讓子與 AI 強度設定。
