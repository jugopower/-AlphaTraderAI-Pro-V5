AlphaTrader AI Pro V5 — Build023 RC2 純 CSS 靜態穩定版

修正重點：
1. 棋盤只依容器寬度決定尺寸，不讀取 Safari 動態高度。
2. 滑動、網址列伸縮及縮放時不執行版面重算。
3. 手機與 iPad 直式只保留一套對局功能列。
4. 控制列全部回到一般文件流，取消固定與黏附定位。
5. 手機版簡化陰影與漸層，降低 Safari 合成負擔。
6. 保留 iPad 橫式完整介面與所有原有功能。
