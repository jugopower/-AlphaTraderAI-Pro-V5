# AlphaTrader AI Pro V5｜Build023 RC2.1 旋轉修正版

本版以 Build023 RC2 為基礎，只修正直式與橫式切換：

- 滑動頁面時不重新計算棋盤。
- 只在裝置方向真正改變後，延遲約 420 毫秒重新適配一次。
- 棋盤、棋子、座標及觸控位置同步重畫。
- 移除開始新局時派送兩次 resize 的舊流程。
- Safari 側邊欄造成的大幅寬度改變，可觸發一次安全重新適配。
- 保留 RC2 的單一控制列與純 CSS 文件流。

建議測試順序：直式開局 → 轉橫式落子 → 再轉回直式 → 上下滑動。
